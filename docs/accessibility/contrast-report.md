# WCAG AA Contrast Report — Speedwave Desktop

This document records the computed contrast ratios for every foreground / background
pair used in the Speedwave Desktop design tokens. Ratios are calculated per the
WCAG 2.1 relative-luminance formula ([W3C contrast definition][wcag-contrast]).

**Thresholds:**

- **AA** body text (≥ 14 px regular / 18 px bold): **4.5 : 1**
- **AA** large text & UI components: **3 : 1**
- **AAA** body text: **7 : 1**
- **AAA** large text: **4.5 : 1**

**Legend:**

| Tag  | Meaning                                       |
| ---- | --------------------------------------------- |
| AAA  | Passes AAA body-text threshold (≥ 7 : 1)      |
| AA   | Passes AA body-text threshold (≥ 4.5 : 1)     |
| UI   | Passes AA large-text / UI threshold (≥ 3 : 1) |
| FAIL | Below 3 : 1 — not accessible at any size      |

Tokens are defined in [`desktop/src/src/styles.css`][styles]. The numbers below
were generated by the relative-luminance formula and cross-checked against
axe-core's `color-contrast` rule (see `desktop/src/src/app/a11y.spec.ts`).

## Ink on background

| Foreground \ Background    | bg-darkest | bg-darker | bg-dark   | bg-navy  | bg-deep   | bg-abyss  | bg-panel  | bg-input  |
| -------------------------- | ---------- | --------- | --------- | -------- | --------- | --------- | --------- | --------- |
| `sw-text` #e0e0e0 (body)   | 12.92 AAA  | 13.89 AAA | 12.04 AAA | 9.47 AAA | 14.85 AAA | 13.18 AAA | 13.73 AAA | 14.72 AAA |
| `sw-text-subtle` #cccccc   | 10.62 AAA  | 11.41 AAA | 9.90 AAA  | 7.78 AAA | 12.21 AAA | 10.83 AAA | 11.29 AAA | 12.10 AAA |
| `sw-text-silver` #c0c0c0   | 9.38 AAA   | 10.07 AAA | 8.74 AAA  | 6.87 AA  | 10.77 AAA | 9.56 AAA  | 9.96 AAA  | 10.68 AAA |
| `sw-text-dim` #aaaaaa      | 7.34 AAA   | 7.89 AAA  | 6.84 AA   | 5.38 AA  | 8.44 AAA  | 7.49 AAA  | 7.80 AAA  | 8.37 AAA  |
| `sw-text-lavender` #a0a0c0 | 6.73 AA    | 7.23 AAA  | 6.27 AA   | 4.93 AA  | 7.73 AAA  | 6.86 AA   | 7.15 AAA  | 7.67 AAA  |
| `sw-text-code` #aaaacc     | 7.58 AAA   | 8.14 AAA  | 7.06 AAA  | 5.55 AA  | 8.71 AAA  | 7.73 AAA  | 8.06 AAA  | 8.64 AAA  |
| `sw-text-faint` #999999    | 5.99 AA    | 6.43 AA   | 5.58 AA   | 4.39 UI  | 6.88 AA   | 6.11 AA   | 6.36 AA   | 6.82 AA   |
| `sw-text-muted` #898989 †  | 4.88 AA    | 5.24 AA   | 4.54 AA   | 3.57 UI  | 5.60 AA   | 4.97 AA   | 5.18 AA   | 5.56 AA   |
| `sw-text-ghost` #909090 †  | 5.34 AA    | 5.74 AA   | 4.98 AA   | 3.91 UI  | 6.14 AA   | 5.45 AA   | 5.68 AA   | 6.09 AA   |
| `sw-accent` #e94560        | 4.46 UI    | 4.79 AA   | 4.15 UI   | 3.26 UI  | 5.12 AA   | 4.54 AA   | 4.74 AA   | 5.08 AA   |
| `sw-teal` #4ecdc4          | 8.82 AAA   | 9.47 AAA  | 8.21 AAA  | 6.46 AA  | 10.13 AAA | 8.99 AAA  | 9.37 AAA  | 10.05 AAA |
| `sw-warning` #fbbf24       | 10.22 AAA  | 10.98 AAA | 9.52 AAA  | 7.49 AAA | 11.74 AAA | 10.42 AAA | 10.86 AAA | 11.64 AAA |
| `sw-success` #2ecc71       | 8.12 AAA   | 8.72 AAA  | 7.56 AAA  | 5.95 AA  | 9.33 AAA  | 8.28 AAA  | 8.63 AAA  | 9.25 AAA  |
| `sw-success-text` #95d5b2  | 10.10 AAA  | 10.86 AAA | 9.41 AAA  | 7.40 AAA | 11.61 AAA | 10.30 AAA | 10.74 AAA | 11.51 AAA |
| `sw-error-text` #e09090    | 6.95 AA    | 7.47 AAA  | 6.48 AA   | 5.09 AA  | 7.99 AAA  | 7.09 AAA  | 7.39 AAA  | 7.92 AAA  |
| `sw-purple-light` #a78bfa  | 6.27 AA    | 6.74 AA   | 5.84 AA   | 4.59 AA  | 7.20 AAA  | 6.39 AA   | 6.66 AA   | 7.14 AAA  |
| `sw-code-lime` #a3e635     | 11.31 AAA  | 12.16 AAA | 10.54 AAA | 8.29 AAA | 13.00 AAA | 11.54 AAA | 12.02 AAA | 12.89 AAA |
| `sw-code-blue` #93c5fd     | 9.46 AAA   | 10.16 AAA | 8.81 AAA  | 6.93 AA  | 10.87 AAA | 9.65 AAA  | 10.05 AAA | 10.78 AAA |
| `sw-code-green` #86efac    | 12.15 AAA  | 13.05 AAA | 11.32 AAA | 8.90 AAA | 13.96 AAA | 12.39 AAA | 12.91 AAA | 13.84 AAA |
| `sw-code-red` #fca5a5      | 8.99 AAA   | 9.66 AAA  | 8.37 AAA  | 6.58 AA  | 10.33 AAA | 9.16 AAA  | 9.55 AAA  | 10.24 AAA |
| `sw-code-gray` #9ca3af †   | 6.72 AA    | 7.22 AAA  | 6.26 AA   | 4.92 AA  | 7.72 AAA  | 6.85 AA   | 7.14 AAA  | 7.66 AAA  |

† Token value bumped during the accessibility sweep; see **Adjustments** below.

### Text on `sw-accent` (#e94560)

| Text color           | on `sw-accent` #e94560 | on `sw-accent-hover` #c23152 |
| -------------------- | ---------------------- | ---------------------------- |
| white (#ffffff)      | 3.83 UI                | 5.45 AA                      |
| bg-darkest (#1a1a2e) | 4.46 UI                | 3.13 UI                      |
| black (#000000)      | 5.48 AA                | 3.86 UI                      |

### `sw-purple-light` on `sw-thinking-bg` (#1e1e3a)

- Used for the "Thinking…" toggle in collapsible thinking blocks.
- Ratio: **5.93 : 1** (AA).

### `sw-purple` on `sw-purple-bg` (#1a1a3e)

- Decorative use only (borders, accents). Never used for body text.
- Ratio: **2.92 : 1** (FAIL for text) — documented waiver below.

## Adjustments applied in this sweep

The following tokens were bumped lighter to pass WCAG AA on the backgrounds
they actually appear on in the Desktop UI:

| Token           | Before  | After   | Reason                                                                      |
| --------------- | ------- | ------- | --------------------------------------------------------------------------- |
| `sw-text-ghost` | #666666 | #909090 | Was 2.97 : 1 on bg-darkest (FAIL). Used as body caption text — needs AA.    |
| `sw-text-muted` | #888888 | #898989 | Was 4.48 : 1 on bg-dark (fails AA by 0.02). One-step bump.                  |
| `sw-code-gray`  | #6b7280 | #9ca3af | Was 3.53 : 1 on bg-darkest and 2.58 : 1 on bg-navy (FAIL). Used for labels. |

Additionally:

- `sw-purple` text on `sw-thinking-bg` (ratio 2.83 : 1, FAIL) was replaced with
  `sw-purple-light` in the `ThinkingBlockComponent` toggle (new ratio 5.93 : 1).

## Waivers

### `sw-purple` on `sw-purple-bg`

- **Usage:** decorative borders and shadows on the ask-user and thinking
  blocks; never carries textual content.
- **Rationale:** WCAG 1.4.11 (non-text contrast) requires 3 : 1 for UI
  components conveying state — the purple border is used alongside a clearly
  labelled text heading (`sw-purple-light`, 5.93 : 1 on the same surface),
  so the decorative purple is supplementary and the 3 : 1 target is still
  met against the broader page background (`bg-darkest`).

### `sw-accent-hover` on card surfaces

- **Usage:** hover background color for primary action buttons with white
  text. White on `sw-accent-hover` is **5.45 : 1** (AA), so the combination
  is accessible as drawn. The hover color itself is not used as foreground.

### `sw-text-faint` / `sw-text-muted` on `sw-bg-navy`

- On `sw-bg-navy` (#0f3460) the ratios drop to 3.57 – 4.39 (UI tier).
- This combination is **anticipated in forthcoming Wave 2-5 sibling PRs and
  is not yet live in the current branch** — no component on `main` or in this
  PR renders `sw-text-faint` or `sw-text-muted` against `sw-bg-navy` today.
- **Font-size context:** these tokens are used at 13 px body-text size,
  so the **4.5 : 1 AA threshold applies** (not the 3 : 1 large-text
  threshold, which only kicks in at ≥ 18 px regular / ≥ 14 px bold).
  The 4.39 : 1 ratio for `sw-text-faint` on `bg-navy` is therefore
  **below AA** for the intended usage, and 3.57 : 1 for `sw-text-muted`
  is even further below.
- **Deferred-validation note (Wave 2-5):** when the Wave 2-5 PRs introduce
  these combinations, the next round must either bump the foreground
  contrast (e.g. by promoting `sw-text-faint` to `sw-text-dim` at 5.38 : 1
  on `bg-navy`) or restrict the combination to ≥ 18 px regular / ≥ 14 px
  bold contexts where the 3 : 1 large-text threshold applies. This
  document will be updated alongside that change; for primary body text
  on navy, components today use `sw-text` (9.47 : 1) or `sw-text-dim`
  (5.38 : 1), so there is no regression in the current branch.

## Theming context — no light theme ships

The terminal-minimal palette is **dark-only by design**. `:root` declares
`color-scheme: dark` and the six accent variants exposed in
`ThemeService` (`crimson` default, `mint`, `amber`, `iris`, `cyan`, `sand`)
rotate ONLY the accent family — backgrounds, ink and lines stay constant.
There is no `[data-theme='light']` selector, no `prefers-color-scheme:
light` media query, and no light-mode token set in the codebase.

The implementation prompt's acceptance criterion #3 says "AXE clean … in
both light and dark modes (if both exist — the mockup is dark-first)".
"If both exist" does not apply here: only dark exists. Per the prompt's
guidance ("the app must honor whichever theme token set ships in its
existing `styles.css`"), the AXE sweep instead iterates every accent
variant the app actually ships, ensuring the contrast and ARIA passes
hold across the full production theme surface — not a hypothetical
light mode.

If a light theme is ever added, this section must be updated with the
new contrast matrix and the `a11y.spec.ts` sweep must add the new
variant to its `THEME_IDS` iteration (it already loops the variant
list verbatim from `ThemeService`).

## How these numbers are verified

1. Ratios computed by the WCAG sRGB relative-luminance formula.
2. `desktop/src/src/app/a11y.spec.ts` runs axe-core `wcag2a`, `wcag2aa`,
   `wcag21a`, and `wcag21aa` rulesets against every routed view —
   ITERATED ACROSS EVERY ACCENT THEME (`crimson`, `mint`, `amber`,
   `iris`, `cyan`, `sand`). The `color-contrast` rule uses the same
   formula as this report.
3. `desktop/src/src/app/forbidden-patterns.spec.ts` asserts that no
   legacy pattern (`*ngIf`, `[ngClass]`, `: any`, `.mutate(`, `TODO`
   marker comments, `@deprecated`, etc.) reappears in `desktop/src/`.

Re-run the suite with `make test-angular` after any token change.

[wcag-contrast]: https://www.w3.org/TR/WCAG21/#dfn-contrast-ratio
[styles]: ../../desktop/src/src/styles.css
