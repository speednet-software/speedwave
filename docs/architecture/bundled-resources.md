# Bundled Resources

Speedwave bundles a set of Claude Code resources that are synced into `~/.speedwave/claude-resources`, mounted read-only into the Claude container at `/speedwave/resources/`, and injected into `~/.claude/` by `entrypoint.sh` at container start. This document is the single reference for **what** is bundled, **how** it reaches the container, and **how** teams can override it.

## Resource Catalog

| Resource                     | Source path                       | Target in `~/.claude/`       | Mechanism               | Overridable?         | Override method                                             |
| ---------------------------- | --------------------------------- | ---------------------------- | ----------------------- | -------------------- | ----------------------------------------------------------- |
| `output-styles/Speedwave.md` | `claude-resources/output-styles/` | `output-styles/Speedwave.md` | symlink (file)[^1]      | No (read-only mount) | Project-level `.claude/output-styles/`                      |
| `statusline.sh`              | `claude-resources/`               | `statusline.sh`              | symlink[^1]             | No (read-only mount) | Project-level `settings.json` with custom command           |
| `settings.json`              | `claude-resources/`               | `settings.json`              | symlink[^1]             | No (read-only mount) | Project-level `.claude/settings.json`                       |
| `mcp-config.json`            | generated by entrypoint           | `mcp-config.json`            | generate (always)[^2]   | No                   | Regenerated on every container start                        |
| `CLAUDE.md`                  | `claude-resources/`               | `CLAUDE.md`                  | symlink[^1]             | No (read-only mount) | Project-level `.claude/CLAUDE.md` or `/workspace/CLAUDE.md` |
| `skills/`                    | `claude-resources/skills/`        | `skills/`                    | symlink (directory)[^1] | No (read-only mount) | Project-level `.claude/skills/` takes precedence            |
| `commands/`\*                | `claude-resources/commands/`      | `commands/`                  | symlink (directory)[^1] | No (read-only mount) | Project-level `.claude/commands/` takes precedence          |
| `agents/`\*                  | `claude-resources/agents/`        | `agents/`                    | symlink (directory)[^1] | No (read-only mount) | Project-level `.claude/agents/` takes precedence            |
| `hooks/`\*                   | `claude-resources/hooks/`         | `hooks/`                     | symlink (directory)[^1] | No (read-only mount) | Project-level `.claude/hooks/` (hooks merge across scopes)  |

**Symlink** means the resource is a symbolic link to the read-only mount at `/speedwave/resources/`. Symlinks are re-created on every container start via `ln -sf` (files) or `ln -sfn` (directories), so they always reflect the latest version shipped with Speedwave. **Generate (always)** means the file is regenerated on every start because it depends on runtime variables (never edit it manually). `mcp-config.json` is the only resource generated inline — all others are symlinked from `claude-resources/`.

\* These directories do not exist in `containers/claude-resources/` yet — the entrypoint logic is ready (`[ -d ... ]` guard) and will activate once they are populated. Symlinks are only created when the source directory exists.

## Bundled Skills

The `skills/` directory ships the `speedwave-code-review` orchestrator and its 13 specialized worker skills. Every production Speedwave install exposes them inside the Claude container as `/speedwave-code-review`, with no per-project setup required.

| Skill                               | Purpose                                                               |
| ----------------------------------- | --------------------------------------------------------------------- |
| `speedwave-code-review`             | Orchestrator — runs the 13 workers in parallel and aggregates results |
| `code-review-basic`                 | Project guidelines and style-guide compliance                         |
| `code-review-security-checker`      | Injection, auth bypass, crypto weaknesses, data exposure              |
| `code-review-silent-failure-hunter` | Silent failures, inadequate error handling, unsafe fallbacks          |
| `code-review-kiss-detector`         | Over-engineering and unnecessary complexity                           |
| `code-review-yagni-detector`        | Speculative features, unused code, premature optimization             |
| `code-review-duplication-detector`  | DRY violations and refactoring opportunities                          |
| `code-review-ssot-detector`         | Single-Source-of-Truth violations (scattered configs, constants)      |
| `code-review-solid-detector`        | SRP, OCP, LSP, ISP, DIP violations                                    |
| `code-review-type-design-analyzer`  | Type design, encapsulation, invariant expression                      |
| `code-review-comment-analyzer`      | Comment accuracy, completeness, long-term maintainability             |
| `code-review-documentation-checker` | Code ↔ documentation synchronization                                  |
| `code-review-test-analyzer`         | Test coverage quality and critical gaps                               |
| `code-review-simplifier`            | Clarity, consistency, and simplification opportunities                |

These skills are the canonical copy of the review workflow developers use on this repo via `.claude/skills/`. Both trees are maintained manually — when editing one copy, update the other.

## Filesystem Layout

After `entrypoint.sh` completes, the container filesystem looks like this:

```
/home/speedwave/.claude/              ← user-level (Speedwave-controlled)
├── output-styles/
│   └── Speedwave.md → /speedwave/resources/output-styles/Speedwave.md  ← symlink (file)
├── statusline.sh    → /speedwave/resources/statusline.sh               ← symlink
├── settings.json    → /speedwave/resources/settings.json               ← symlink
├── CLAUDE.md        → /speedwave/resources/CLAUDE.md                   ← symlink
├── mcp-config.json                   ← generated (MCP hub endpoint)
├── skills/    → /speedwave/resources/skills/     ← symlink (when dir exists)
├── commands/  → /speedwave/resources/commands/   ← symlink (when dir exists)
├── agents/    → /speedwave/resources/agents/     ← symlink (when dir exists)
├── hooks/     → /speedwave/resources/hooks/      ← symlink (when dir exists)
└── ide/                              ← read-only mount (IDE Bridge lock files)

/workspace/.claude/                   ← project-level (optional, team-committed via git — Speedwave never creates this)
├── agents/
├── commands/
├── skills/
├── settings.json
└── CLAUDE.md
```

The volume mounts that produce this layout are defined in `compose.template.yml`:

```yaml
volumes:
  - ${CLAUDE_HOME}:/home/speedwave:rw # persistent home
  - ${PROJECT_DIR}:/workspace # project directory
  - ${RESOURCES_DIR}:/speedwave/resources:ro # bundled resources (read-only)
  - ${IDE_LOCK_DIR}:/home/speedwave/.claude/ide:ro # IDE Bridge lock files (read-only)
```

## Bundle Reconcile Lifecycle

The desktop bundle ships `build-context/containers/claude-resources/` plus a generated `bundle-manifest.json`. On app startup, the backend compares the installed `bundle_id` with `~/.speedwave/bundle-state.json`.

When the bundle changed, the desktop app:

1. Validates the bundled `claude-resources`
2. Atomically syncs them into `~/.speedwave/claude-resources`
3. Rebuilds built-in images for the current `bundle_id`
4. Recreates previously running projects if the update flow stored them in `pending_running_projects`

This means compose always mounts the stable host path `~/.speedwave/claude-resources`, but the content of that directory is managed by the startup reconcile rather than read directly from the installed app bundle on every container start.

## Bundle Asset Gate

Desktop packaging now treats the staged Tauri resources as a validated runtime contract, not a best-effort copy step. Before `cargo tauri build` runs, the desktop build verifies:

- bundled `build-context/` and `mcp-os/` trees exist and are non-empty
- the platform runtime is present (`node`, CLI, Lima or nerdctl/WSL assets)
- macOS bundles include `reminders-cli`, `calendar-cli`, `mail-cli`, and `notes-cli`
- the matching `tauri.<platform>.conf.json` actually declares those resources for bundling

This closes the failure mode where a release could build successfully even though a runtime helper was missing from the final app bundle.

## Scope Hierarchy

Claude Code resolves settings and resources using a scope hierarchy where **project-level takes precedence over user-level**:

| Scope   | Container path             | Source                | Priority |
| ------- | -------------------------- | --------------------- | -------- |
| Project | `/workspace/.claude/`      | Team's git repository | Higher   |
| User    | `/home/speedwave/.claude/` | Speedwave bundle      | Lower    |

This means teams can override any Speedwave resource by committing a same-named file to `.claude/` in their repo. Hooks are an exception — hook arrays merge across scopes (both user-level and project-level hooks fire).

**Note on `settings.json`:** The user-level `~/.claude/settings.json` is a symlink to the read-only mount, so Claude Code cannot persist settings changes there (writes fail with EPERM). Teams that need custom settings (e.g., enabling/disabling permissions, configuring thinking) should commit a `.claude/settings.json` at the project level — project-level settings take precedence over user-level for the same keys.

See [ADR-022](../adr/ADR-022-bundled-claude-resources-and-project-coexistence.md) for the full design rationale and rejected alternatives.

## Plugin Resources

When plugins are installed (`SPEEDWAVE_PLUGINS` env variable), `entrypoint.sh` symlinks individual files and directories from each plugin into the user-level directories. Plugin directories are mounted into the container at `/speedwave/plugins/<plugin-slug>/`:

```
/speedwave/plugins/<plugin-slug>/        ← container path (host: ~/.speedwave/plugins/<plugin-slug>/claude-resources/)
├── commands/    ← per-file symlinks into ~/.claude/commands/
├── agents/      ← per-file symlinks into ~/.claude/agents/
├── skills/      ← per-file symlinks into ~/.claude/skills/
└── hooks/       ← per-file symlinks into ~/.claude/hooks/
```

Unlike core resources (which symlink entire directories), plugin resources create per-entry symlinks using `[ -e ]` and `ln -sfn`, supporting both files and directories. This allows core and plugin resources to coexist within the same user-level directory. Plugin resources support all four resource types: `commands`, `agents`, `skills`, and `hooks`. Plugins run inside the container (sandboxed with `cap_drop: ALL`, `no-new-privileges`, `read_only`), so plugin hooks have the same trust boundary as plugin skills and commands.

See [ADR-015](../adr/ADR-015-plugin-system.md) for the plugin system design.

## Persistence Across Container Restarts

The Claude container's home directory (`/home/speedwave/`) is backed by a persistent volume (`${CLAUDE_HOME}:rw`). This affects resource behavior on restart:

- **Symlinked resources** (`statusline.sh`, `settings.json`, `CLAUDE.md`, `output-styles/Speedwave.md`, `skills/`, `commands/`, `agents/`, `hooks/`) — re-created on every start via `ln -sf` (files) or `ln -sfn` (directories). They point to the read-only mount, so their content updates automatically when Speedwave ships new versions. Teams override via project-level `.claude/` (ADR-022 scope precedence).
- **Always-regenerated resources** (`mcp-config.json`) — overwritten on every container start, because the MCP hub port may change between runs. This is the only resource generated inline by `entrypoint.sh`.
- **Host-side resource sync** (`~/.speedwave/claude-resources`) — updated only when `claude_resources_hash` changes in the installed bundle manifest. The sync uses a staging directory plus rename, so the compose mount never points at a partially copied resource tree.

## Adding a New Bundled Resource

Checklist for contributors:

1. Add the file or directory to `containers/claude-resources/`
2. Add symlink logic in `containers/entrypoint.sh` (follow existing patterns — `ln -sf` for individual files, `ln -sfn` for directories)
3. Add BATS tests in `_tests/entrypoint/` covering the new resource (verify with `[ -L ... ]` and `readlink`)
4. Update the [Resource Catalog](#resource-catalog) table in this document
5. Run `make test-entrypoint` to verify

## See Also

- [ADR-022 — Bundled Resources and Project Coexistence](../adr/ADR-022-bundled-claude-resources-and-project-coexistence.md)
- [ADR-015 — Plugin System](../adr/ADR-015-plugin-system.md)
- [Containers](containers.md) — OCI images, compose templates, per-project isolation
- [Security Model](security.md) — container hardening, token isolation

---

[^1]: `entrypoint.sh` uses `ln -sf` (files) or `ln -sfn` (directories) to atomically create or replace symlinks on every start. The target is on a read-only mount (`/speedwave/resources/:ro`), so resources auto-update when Speedwave ships new versions.

[^2]: `mcp-config.json` is always overwritten because the MCP hub port may change between runs (via `MCP_HUB_PORT` env). This is the only resource generated inline by `entrypoint.sh`.
