---
paths:
  - 'crates/**'
  - 'desktop/**'
  - 'mcp-servers/**'
  - 'containers/**'
  - 'scripts/**'
  - 'native/**'
  - '_tests/**'
  - '.github/**'
---

# Alignments — keep paired sources in sync

Two kinds: **test-guarded** (a failing test names the fix — trust it, never bypass it) and **manual** (no automated guard — update both sides in the same commit or it drifts silently). When in doubt about whether a pair is guarded, grep for the test name; if none exists, treat it as manual.

## Test-guarded alignments — the failing test names the fix

- Resource tables ↔ compose template placeholders — `compose::tests::resources_render_from_ssot`.
- Every rendered compose passes the full OWASP/zero-token gate — `test_rendered_compose_passes_security_check` + the `test_security_check_*` family (cap_drop/no-new-privileges/read_only/tmpfs/user, no tokens on claude or hub, localhost-only ports, external-LLM-key deny list, per-worker volume profiles, and the `ManagedSettingsMount` profile — `:ro` at the exact `/etc/claude-code/managed-settings.json` target from `<data_dir>/claude-managed/<project>/`). Adding a `SecurityRule` variant = bumping `SECURITY_RULE_COUNT` (guarded by `test_all_rules_covers_every_variant` / `test_security_rule_iter_first_and_last`).
- Compose template services ↔ `consts::BUILT_IN_SERVICES` — `test_internal_only_covers_all_template_services`.
- `HOST_GATEWAY_ALIAS` ↔ `mcp-servers/shared/src/security.ts` ↔ template `extra_hosts` (static on `claude`, `proxy`, `mcp-playwright`) — `host_gateway_alias_matches_mcp_shared_ts`, `host_gateway_alias_appears_in_compose_template`, `compose_template_extra_hosts_contains_only_canonical_alias`.
- ~~Rust `PiiCategory` serde wire strings ↔ TS `PIIType` enum~~ — removed: the resolver (`pii_policy.rs`) moved to an open rule-id set (no fixed enum) in the v3 PII policy migration; `pii_category_serde_matches_policy_engine_ts` no longer exists. The orphaned TS `PIIType` enum (`mcp-servers/policies/src/types.ts`) has been deleted (design decision 2.3, `SPEED-311-yaml-rules-design.md`) — done. The Desktop Settings UI now sources categories dynamically via `list_pii_rules` (`rule_library()`) instead of a fixed enum; see `rules_yaml_is_ssot_for_pii_categories` (`pii_policy.rs`).
- `POLICY_FILE`/`POLICY_DIGEST` env names ↔ `compose.template.yml` mcp-hub env ↔ `mcp-servers/hub/src/policy.ts` reader: `spw_policy_env_names_appear_in_compose_template`, `hub_policy_volume_appears_in_compose_template` (`security_check.rs`).
- `desktop/src/angular.json` serve `port` ↔ `desktop/src-tauri/tauri.conf.json` `devUrl` port, and `make dev` strips `PORT` before launching Tauri — `_tests/desktop/dev-server-port.bats`. `DEV_PORT` overrides both at once through `TAURI_CONFIG` (`build.devUrl` + `build.beforeDevCommand`), so the pair cannot drift; without it the two files stay the only source.
- Windows dev setup path budget alignment:
  - `setup-dev-windows.ps1` generates a crate-local `target-dir` that must fit `check-vulkan-path-budget.sh` limits (`SUFFIX_BUDGET`/`MAX_PATH`).
  - Allowed script outputs: `~/msvc-env.sh`, `~/.bashrc`, and `desktop/src-tauri/.cargo/config.toml`.
  - Never write to the committed `<repo>/.cargo/config.toml` (pins `SPEEDWAVE_DATA_DIR`).
  - Shared resolver: `scripts/cargo-target-dir.sh` serves both the path budget gate and `Makefile`'s `E2E_BINARY`.
  - Tests: `_tests/desktop/setup-dev-windows.bats`, `_tests/desktop/vulkan-scripts.bats` and `_tests/desktop/check-windows-build-deps.bats`.
- `.node-version` ↔ every `node-version-file:` in `.github` ↔ Makefile `NODE_VERSION` (bundled Node, `make setup-dev` gate via `scripts/check-node-version.sh`) — `_tests/ci/node-version-pin.bats` (also forbids a `.nvmrc` shadow copy and inline `node-version:` literals). Make recipes run the caller's node (nvm, `actions/setup-node`): the Makefile `PATH` export puts `$(HOME)/.cargo/bin` first (rustup honours `rust-toolchain.toml`) and `/opt/homebrew/bin` last, because macOS runner images `brew link` their own `node@<major>` there — `_tests/ci/makefile-path-precedence.bats`.
- No hardcoded loopback binds — drift detector `tests/no_hardcoded_loopback_bind.rs`; no raw engine paths — `tests/no_raw_engine_path.rs` (escape hatch for both: `// SSOT-allow: <reason>`).
- `LockedRuntime` encapsulation (trait stays `pub(crate)`, no lock re-exports) — `tests/ssot_enforcement.rs`.
- `consts::SLACK_OAUTH_TOKEN_URL` ↔ oauth worker `providers/slack.ts` — `slack_token_url_matches_oauth_worker_provider_ts`.
- Plugin slug regex Rust ↔ oauth worker TS — `plugin_slug_pattern_matches_oauth_state_ts`; auth field types Rust ↔ TS — `allowed_auth_field_types_match_ts_union`.
- `build.rs::IMAGES` ↔ `scripts/bundle-build-context.{sh,ps1}` — `bundle_build_context_sh_covers_all_worker_images`, `bundle_scripts_service_lists_are_in_sync`; `${IMAGE_*}` placeholders ↔ catalog — `image_placeholders_align_with_catalogue_and_template`; COPY sources ↔ `hash_inputs` — `hash_inputs_cover_copy_sources`. Bundle-script output tree also pinned by `_tests/desktop/bundle-build-context.bats`.
- Bundle-script env knobs `.sh` ↔ `.ps1` (`BUNDLE_DEST` = staging dir; `BUNDLE_MCP_SERVERS_DIR` and `BUNDLE_CONTAINERS_DIR` = the source trees the bundle is staged from, `--ci` rebuilding the former; `BUNDLE_WASM_PKG_DIR` = the pii-engine wasm out dir, with its `.wasm-build.lock` beside it) — `bundle_scripts_env_knobs_are_in_sync` pins that both scripts read all four with the same default and never name that default path directly; the lock placement beside the out dir is manual on the `.ps1` side. Every writer of a wasm out dir takes that lock through `scripts/mkdir-lock.sh`: the bundle scripts hold it from start to exit and call `crates/pii-engine-wasm/build-wasm.sh` plain, while npm's `build:wasm` passes `--lock`. `build-wasm.sh` anchors a relative out dir to the caller's cwd, because the `.ps1` passes one, and keeps the absolute path forms of `scripts/cargo-target-dir.sh`. `bundle-build-context.bats` pins these and relies on all of this to never write the real tree: every test stages into its own temp DEST and wasm out dir, and fixtures go into scratch copies of the tracked `containers/`/`mcp-servers/` files. That is what lets `make test` run `test-mcp-run`, `test-desktop-build-run` and `test-desktop-run` as independent parallel lanes.
- `bundle.rs::HOST_BUILD_OUTPUT_DIRS` ↔ bundle scripts ↔ `containers/.dockerignore` — `host_build_output_dirs_align_with_bundle_scripts_and_dockerignore`: every `-prune` line in the `.sh` (`-iname`) and every `-in` line in the `.ps1` must name exactly those dirs, `copy_tree`/`Copy-Tree` must skip them case-insensitively together with links carrying those names (`-type l`, `ReparsePoint`), never `rm`/`Remove-Item` or copy recursively, and both scripts must stage the same source trees. Matching ignores case everywhere (scripts, the digest, `tests/apt_noninteractive.rs`) because NTFS and APFS resolve `Target` and `target` to one directory. Integration tests read the names through `bundle_test_support` (`test-support` feature), so the const stays `pub(crate)`.
- Windows production literals (`"Speedwave"` distro, `.speedwave`, `nodejs/`, `bin/`, `resources/`) across installer template, sweep.ps1, e2e-vm.sh — `consts.rs` `*_appears_in_*` tests + `tauri_windows_resources_subdir_matches_desktop_layout`.
- Installer/sweep/firewall pipeline (generated `installer-hooks.nsh`, kill targets, firewall modes/engines, WiX sequencing, every hook PowerShell run going through the window-less `SPEEDWAVE_RUN_HIDDEN` launcher) — the tests in `desktop/src-tauri/src/installer_hooks.rs`; `scripts/check-nsis-update-reset.ps1` runs the real `/P /UPDATE` install from a `TEMP` whose path contains a space. `sweep.ps1` itself (kill filter, lock probe, runtime mode, exit codes 2/3/4) runs under `pwsh` in `_tests/desktop/installer-sweep.bats` with the process list stubbed; its exit code decides whether PREINSTALL runs the reset, so a probe that reports a lock nobody holds silently disables the reset. Editing a `.ps1` requires `make generate-installer-nsh`.
- `tauri.windows.conf.json` directory resources plus `RETIRED_DIRECTORY_RESOURCE_ROOTS` ↔ the `$trees` list of `desktop/src-tauri/windows/reset.ps1` — `reset_ps1_clears_every_current_and_retired_directory_resource`; every directory-kind asset of `bundle::required_bundled_assets("windows")` ↔ those trees — `reset_trees_cover_every_bundled_directory_asset`; the Desktop binary name (`mainBinaryName`, else the crate name) ↔ `sweep.ps1` and `reset.ps1` — `sweep_ps1_kills_all_three_target_categories`, `reset_ps1_resets_only_the_default_install_dir_while_the_desktop_is_stopped`; PREINSTALL runs the reset only after a successful sweep and never through NSIS `RMDir`, which recurses through junctions — `preinstall_runs_the_reset_only_after_a_successful_sweep` (all `installer_hooks.rs`). The script's guards and link handling run under `pwsh` in `_tests/desktop/installer-reset.bats`. An `/UPDATE` or silent NSIS install never runs the previous uninstaller, so a tree the reset misses keeps every file a release drops, and that file reaches the image build (ADR-048 amendment). Retiring a directory resource = adding its root to both `RETIRED_DIRECTORY_RESOURCE_ROOTS` and `$trees`.
- Every `-File "[INSTALLDIR]….ps1"` in `windows/sweep.wxs` and `windows/firewall.wxs` ↔ a resource target in `tauri.windows.conf.json` — `msi_custom_actions_run_the_scripts_where_the_msi_installs_them` (`installer_hooks.rs`). The WiX bundler installs each resource at its target directly under `INSTALLDIR` (no `resources\` parent), so a path that is not a target makes the custom action fail silently.
- `bundle::SHIPPED_FILES_LIST` / `bundle::SHIPPED_TREES` ↔ `scripts/bundle-build-context.{sh,ps1}` — `bundle_scripts_write_the_shipped_files_list_into_every_shipped_tree`; the list matches the staged tree — `_tests/desktop/bundle-build-context.bats`; the installed trees match their lists on a real Windows install — `scripts/check-nsis-update-reset.ps1` in `desktop-build.yml`. Both scripts must emit the same bytes (sorted ordinally, LF): the release is bundled with the `.sh` under Git Bash, the Windows rig with the `.ps1`.
- nerdctl pin (`.lima-version` ↔ `NERDCTL_FULL_VERSION` ↔ e2e-vm.sh URL) — `lima_version_and_nerdctl_full_version_are_aligned`, `nerdctl_version_appears_in_e2e_vm_script`. (macOS and Windows nerdctl versions are NOT string-equal — the lima→nerdctl table in `consts.rs` maps them.)
- nerdctl name-store dir / compose-prefix / WSL-distro derivation / dirty-state token-path literals shared between the Rust SSOT and the e2e TS/bats harness — `name_store_dir_literal_matches_e2e_engine_ts_and_bats_suites` (`consts.rs`'s `NERDCTL_DATA_ROOT`/`nerdctl_addr_hash()`/`CONTAINERD_NAMESPACE` path ↔ `engine.ts`'s `nameStoreDir()` ↔ `engine-contract.bats`/`update-dirty-state.bats` `STORE=`), `engine_ts_compose_prefix_and_wsl_distro_derivation_mirrors_consts` (`derive_instance_name_from`/`derive_wsl_distro_name_from` ↔ `engine.ts`'s `composePrefix()`/`wslDistroName()`), `dirty_state_spec_service_token_path_matches_tokens_and_workers_shape` (`compose/tokens.rs`'s secrets-dir name + `compose/workers.rs`'s token-filename suffix ↔ `19-dirty-state-self-heal.spec.ts`'s `serviceTokenPath()`), `update_dirty_state_bats_prefix_derivation_mirrors_compose_prefix` (`update-dirty-state.bats` `PREFIX` derivation ↔ `compose_prefix()`).
- Compose up heal/deadline phrases ↔ the engine contract — `engine_contract_bats_pins_the_task_collision_and_up_deadline_phrases` (`runtime/mod.rs`'s `TASK_BUNDLE_DIR_FRAGMENT` + `CONTAINERD_NAMESPACE` ↔ `engine-contract.bats` `TASKS=`, `TASK_ALREADY_EXISTS_FRAGMENT` ↔ its paused-container `task $PAUSED_ID: already exists` assertion, and `COMPOSE_UP_DEADLINE_FRAGMENT` ↔ its `timeout --signal=KILL --verbose` assertion). The e2e restart wait ↔ the up deadline — `e2e_restart_wait_outlasts_an_up_stopped_at_the_deadline_and_its_rollback` (`desktop/e2e/helpers/shell.ts`'s `RESTART_WAIT_MS` ≥ 2 × `COMPOSE_UP_TIMEOUT_SECS`); the mocha caps of the tests that restart are written as `RESTART_WAIT_MS + …` by hand, with no guard.
- Windows signing (ADR-086): `sign-windows-binaries.ps1` `$SignTargets` ↔ `tauri.windows.conf.json` `bundle.resources`, plus both Tauri hooks (`build.beforeBundleCommand` `-Bundled`, `bundle.windows.signCommand` `%1`) — `_tests/desktop/sign-windows-binaries.bats`. The OIDC login/env contract (`azure-signing-login/action.yml` ↔ the `desktop-release.yml` jobs ↔ the script's `AZURE_ARTIFACT_SIGNING_*` env) — `_tests/desktop/release-workflow-signing.bats`. Every permission a `desktop-release.yml` job requests ↔ the `permissions` of the `build-and-publish` job in `release-please.yml` that calls it: a called workflow cannot exceed its caller's `GITHUB_TOKEN` grant, and a missing grant makes GitHub reject the whole release workflow at startup, so release-please never runs — same suite. Every `azure-signing-login` step in `desktop-release.yml` is followed at once by `az account get-access-token --resource https://codesigning.azure.net`: the GitHub OIDC client assertion expires 5 minutes after the login, and Azure CLI mints the Artifact Signing token from it only when first asked, which in `publish-tauri` is Tauri's `beforeBundleCommand` about 20 minutes later (`AADSTS700024`), so the token is fetched and cached while the assertion is valid. The fetch lives in the workflow file and not in the composite action or the script, because a `workflow_dispatch` rebuild of an existing tag reads the workflow from `main` but checks out the action and the script from the tag — same suite. Every file the workflow hands to `sign-windows-binaries.ps1` is a rooted path (`$env:GITHUB_WORKSPACE\…`): `Invoke-ArtifactSigning` fails a relative one with `The file path '…' is not rooted` before it contacts the service — same suite.
- macOS signing: `sign-bundled-binaries.sh` `SIGN_TARGETS` ↔ `tauri.macos.conf.json` `bundle.resources` ↔ entitlements plists — `_tests/desktop/sign-bundled-binaries.bats`. Native CLI Info.plists (version ↔ tauri.conf.json, sub-identifiers, TCC descriptions, single-key entitlements, the main app's mic entitlement, audio-capture bundling) — `_tests/desktop/{native-cli-info-plist,info-plist,entitlements-reminders,main-app-entitlements,transcription-bundle}.bats`, all in `DESKTOP_BUILD_BATS`. `native-cli-info-plist.bats` must stay bash 3.2-clean (macOS runners ship no newer bash); its embedded-plist half skips as a block unless `make build-native-macos` has run.
- Versioned files: `release-please-config.json` `extra-files` is the single list; `backmerge.yml` derives its resolve/exclude sets from it via jq — `_tests/desktop/backmerge-alignment.bats`, `_tests/desktop/version-consistency.bats`. New versioned artifact = add it to `extra-files`, nothing else. A `generic` extra-file is bumped through an `x-release-please-version` comment on the version line, so any build step that rewrites such a file must preserve its comments — `_tests/desktop/build-native-macos.bats` pins that for the five native CLI plists (see native-macos rules).
- Updater config (endpoint ↔ `updater.rs::STABLE_ENDPOINT`, minisign pubkey, v1Compatible artifacts) — `_tests/desktop/updater-config.bats`. Release pipeline signing + asset/sig/latest.json completeness — `_tests/desktop/{release-workflow-signing,verify-release-assets}.bats`.
- User-message wire envelope (Rust side) — snapshot `build_user_message_snapshot_wire_format`.
- Model catalog pricing completeness — `tests/anthropic_pricing_completeness.rs`; timeout budgets (MCP idle ≥ worker stall; Lima provision == Desktop reconcile wait) — `mcp_tool_idle_timeout_covers_worker_max`, `lima_provision_start_timeout_matches_desktop_reconcile_wait_budget`.
- `AnthropicModelInfo` (Rust) ↔ `AnthropicModel` (TS `models/llm.ts`) field set — `anthropic_model_wire_fields_match_ts_mirror` (`types.rs`); per-model `effort_levels`/`default_effort` pinned against the docs table and checked as a `EFFORT_LEVELS`-order subset — `anthropic_models_effort_table_matches_docs`, `anthropic_models_effort_levels_are_subsets_of_effort_levels_in_order` (`defaults.rs`); the per-model 1M-by-plan policy is pinned against the ADR-089 table by `one_million_context_table_matches_the_plan_decisions` (`defaults.rs`).
- Control-response fixture ↔ `defaults.rs::CLAUDE_VERSION`: `fixture_is_the_capture_of_the_pinned_claude_code` (`control_channel.rs`) fails on a Claude Code bump until `desktop/src-tauri/tests/fixtures/cc-<version>-control-responses.sanitized.json` is re-captured from the new binary; the picker, plan-usage and context parsers are all asserted against that capture.
- Soft-impose stream captures ↔ `defaults.rs::CLAUDE_VERSION`: `the_soft_impose_captures_are_of_the_pinned_claude_code` (`chat.rs`) fails on a Claude Code bump until `cc-<version>-soft-impose.sanitized.ndjson` (a `set_model` control request sent at the first `init` of a tool-using turn, then the next message), `cc-<version>-model-command-mid-tool-turn.sanitized.ndjson` (a `/model` input written at the same point) and `cc-<version>-model-picks.sanitized.ndjson` (`set_model` with an Anthropic id and with `default`, then a typed `/model`) are re-captured. `chat.rs::soft_impose_step` is run over the first. The second and the typed `/model` of the third pin why no model switch is a `/model` input: both answer as a turn of their own, the one written during a tool-using turn after that turn ends.
- Safety-check capture ↔ `defaults.rs::CLAUDE_VERSION`: `the_safety_check_capture_is_of_the_pinned_claude_code` (`chat.rs`) fails on a Claude Code bump until `cc-<version>-safety-check.sanitized.json` is recorded again: a `can_use_tool` request with `decision_reason_type: safetyCheck` raised under `--dangerously-skip-permissions` for `rm` on a command substitution, the `deny` Speedwave answers, the failed tool result the model receives, and that the `rm` target survived. The tests over it pin that `build_tool_permission_response` declines such a request with Claude Code's reason, that the recorded answer is exactly the one it builds today (so a new decline text needs a new recording), and that the model gets the decline as an `is_error` tool result (security rules).
- `set_model` check capture ↔ `defaults.rs::CLAUDE_VERSION`: `the_set_model_check_capture_is_of_the_pinned_claude_code` (`chat.rs`) fails on a Claude Code bump until `cc-<version>-set-model-check.sanitized.json` is recorded again. The tests over it pin that a `set_model` sent before the first user message moves the first `init` and the first request (why the soft-impose goes out at the spawn), that a refused one leaves the launch model, that Claude Code answers an unanswered one-token check before `control_channel::SET_MODEL_TIMEOUT`, and that a confirmed typed `/model` answers `Set model to …` while a refused one answers something else (`refused_model_command`).
- `control_channel::MODEL_SWITCH_FAILED_EVENT` and its payload `control_channel::ModelSwitchFailedEvent` ↔ `models/claude-control.ts::ClaudeModelSwitchFailedEvent` and `CLAUDE_MODEL_SWITCH_FAILED_EVENT` — `model_switch_failed_event_matches_ts_mirror`.
- `apply_flag_settings` capture ↔ `defaults.rs::CLAUDE_VERSION`: `the_apply_effort_capture_is_of_the_pinned_claude_code` (`control_channel.rs`) fails on a Claude Code bump until `cc-<version>-apply-effort.sanitized.json` is recorded again. The recording holds five things:
  - Claude Code's answers to `apply_flag_settings` with `low`, `max` and `turbo`;
  - the `output_config.effort` of each turn after them;
  - whether a `settings.json` already in the config directory changed, and which keys `.claude.json` gained, with their values;
  - a request sent before the first user message by a process launched with `--effort high`, that launch level, and the effort of the first turn;
  - an `/effort` input written during a tool-using turn: the `num_turns` of every `result` and the effort of each request.

  The tests over it pin six facts: an effort change applies from the next model request; it applies before the first turn too; it writes no settings; it stores nothing in `.claude.json`; an unknown level is answered `success` without effect, which is why `apply_chat_effort` validates the level first; and what an `/effort` input written mid-turn does, which is why no effort change is such an input.

- Resume captures ↔ `defaults.rs::CLAUDE_VERSION`: `the_resume_captures_are_of_the_pinned_claude_code` (`history.rs`) fails on a Claude Code bump until `cc-<version>-resume-transcript.sanitized.jsonl`, `-resume-transcript-after.sanitized.jsonl` and `-resume-stdout.sanitized.ndjson` are recorded again. Over them, the resume snapshot starts from the last `cost-state` line Claude Code wrote at exit, since the transcript holds no `result` line. `a_resumed_first_turn_is_charged_only_for_itself` then shows that the first turn after `--resume` costs only itself, and not the whole session; its expected cost and tokens are derived from the captures, so a re-capture on another default model still checks behaviour. The `cost-state` schema the snapshot accepts (`history.rs::CostState`) mirrors the one the pinned binary restores from, which no test can read; the bump procedure re-checks it (architecture rules).
- `apt-get install` must set `DEBIAN_FRONTEND=noninteractive` in every Containerfile/Dockerfile — `tests/apt_noninteractive.rs`.
- Tests never touch production `~/.speedwave` — `tests/no_raw_data_dir_in_tests.rs`, `tests/prod_data_dir_untouched.rs`, `_tests/desktop/guard-prod-data-dir.bats`.
- Plugin signature is a runtime invariant (every plugin-tree read goes through `signing::verify_plugin_signature_cached`) — e2e tamper guard `_tests/e2e/plugin-tamper.bats`.
- Integration resource link/unlink on toggle — `_tests/entrypoint/entrypoint.bats`.
- claude-resources `integrations/<dir>` names ↔ descriptor `config_key`s — `integrations_directories_match_known_service_keys`.
- Service descriptors ↔ resolved-config fields / `BUILT_IN_SERVICE_IDS` / Desktop OS-config getter / native-CLI resolver — `*_covers_all_toggleable_services`, `*_matches_resolved_config_fields`, `resolve_native_cli_binary_covers_all_os_services`.
- Repo `.speedwave.json` env deny-predicate covers `RESERVED_ENV_KEYS` + Anthropic keys — `test_repo_env_key_is_denied_covers_ssot`.
- Displayable `DIAGNOSTIC_SOURCES` ↔ /logs merge parity — `logs_view_covers_all_displayable_registry_sources`.
- Desktop SharePoint required-scopes ↔ `consts::SHAREPOINT_OAUTH_SCOPES` — `sharepoint_required_scopes_matches_ssot_lowercased`.
- `.clipboard-bridge` filename Rust ↔ `containers/osc52-copy.sh` — `bridge_filename_matches_shell_wrapper_literal` + `_tests/entrypoint/osc52-copy.bats`.
- `containers/entrypoint.sh`'s foreign-model alias alternation ↔ `defaults.rs::CLAUDE_CODE_MODEL_ALIASES` — `entrypoint_foreign_model_regex_matches_claude_code_model_aliases`; the runtime half is `_tests/entrypoint/entrypoint.bats`, which reads the alias list out of the script under test. `defaults.rs::is_claude_code_model_setting` applies the same pattern in Rust (the check behind `claude_settings::restore_model_pin`), so a change to the pattern that test expects changes that function too.
- e2e-vm.sh rsync excludes ↔ bundled-asset dirs + `ps_squote` on every windows_ps env — `_tests/e2e/e2e-vm-excludes.bats`.
- CI runtime coverage ↔ Makefile gates: the lint job runs `make check-clippy` verbatim (never a hand-rolled clippy), `runtime-windows` runs `test-rust`'s exact cargo invocation (full suite + test-support), `check-clippy` keeps `--all-targets` + the feature pass, and the audit job runs `make audit-rust` verbatim (no hand-rolled `cargo audit`, so `AUDIT_IGNORE` stays single-homed) — `_tests/ci/rust-coverage-gates.bats`.
- Angular unit-test config ↔ CI Angular coverage ↔ Makefile gate: the test target in `desktop/src/angular.json` sets `runnerConfig` to `vitest.config.ts`, and the `Angular tests + coverage` step in `test.yml` runs the exact `ng test` line and directory that `make -n coverage-angular` prints, with no `--runner-config` override (run directly, so setup-node's pinned Node runs it) — `_tests/ci/angular-coverage-gates.bats`.
- Every `test.yml` job ↔ `ci-gate.needs` — `_tests/ci/ci-gate.bats`: `ci-gate` runs `if: always()`, keeps its job id as the check context, carries the only `if:` in the file (a job- or step-level `if:` anywhere else fails the guard, because `scripts/ci-gate.sh` treats `skipped` and `cancelled` like `failure`), and is fed `toJSON(needs)` (flow or block `needs:` form). A new job is gated by adding it to `needs`; the branch ruleset never changes.
- `chat_session_cmd.rs::MSG_SESSION_KEPT` ↔ `chat-state.service.ts::SESSION_KEPT_MARKER`, the prefix of a start refused before the running session stopped, which decides whether a failed New chat or resume returns to the running conversation and keeps its queued picks — `session_kept_marker_matches_ts`; every `?` before the swap in `start_session_inner` carries it — `every_failure_before_the_old_session_stops_says_it_was_kept`.
- `chat_session_cmd.rs::MSG_SESSION_BUSY` and `MSG_SESSION_REPLACED` ↔ the send-retry triggers in `chat-state.service.ts::sendMessage` (its `errStr.includes('…')` literals): neither error may match them, or lock contention makes the retry replace a live conversation and a message refused because its session was replaced goes into the conversation that replaced it — `send_retry_triggers_in_ts_never_match_the_busy_error`.
- Rust↔TS type mirrors require `include_str!` cross-read unit tests (grep `_matches_ts` / `_matches_rust`):
  - `models/telemetry.ts` mirrors: `host_gateway_alias`, `slack_token_url`, `plugin_slug`, `allowed_auth_field_types`, `llm_provider_kind`, `cost_source`, `max_expires_in`, `save_oauth_state_key_set`, `max_credential_bytes`, `discovery_err_contract`, `otlp_protocol`, `telemetry_locks_field_set`.
  - `models/transcript.ts` mirrors: `mic_permission`, `mic_permission_status`, `live_draft`, `transcript_source`, `audio_parts`, `active_warnings`, `gpu_class`, `CapabilitiesAck` field set, and `audio_level` (`audio_level_serde_round_trip_and_ts_mirror` in `transcript_store.rs`).
  - `services/project-state.service.ts` mirror: `oauth_sign_in` (`OauthSignIn` union, `oauth_sign_in_matches_ts_union` in `types.rs`).
  - `models/claude-control.ts` mirrors (ADR-089): the control payloads, the session-info union and the model-switch outcome (`control_payloads_match_ts_mirrors`, `session_info_state_tags_match_ts_union`, `model_switch_outcome_tags_match_ts_union` in `control_channel.rs`); `models/chat.ts::RateLimitInfo` (`rate_limit_chunk_fields_match_ts_mirror` in `chat.rs`).
  - `models/model-picker.ts` mirrors: the picker rows (`model_picker_matches_ts_mirror` in `model_picker.rs`) and the canonical-id function, which both sides run over the `CANONICAL_CASES` table of `model-picker.spec.ts` (`canonical_model_id_matches_ts`).
  - `models/llm.ts::ActiveProviderSummary` ↔ `containers_cmd::ActiveProviderSummary` field set, `effort_levels` included (`active_provider_summary_matches_ts_mirror` in `containers_cmd.rs`).
  - Dynamic exceptions: `models/security-policy.ts` no longer mirrors a fixed Rust enum (`pii_category_matches_models_security_policy_ts` was removed with `PiiCategory`). Its category IDs are sourced at runtime from `list_pii_rules`, guarded by `rules_yaml_is_ssot_for_pii_categories` in `pii_policy.rs`.
  - Requirement: Add an `include_str!` cross-read test for any new fixed mirror.
- Smaller SSOT pins are unit-guarded (entrypoint MCP config path, `ANTHROPIC_DEFAULT_*` model env ↔ catalog latest flags, proxy recreate digest covers proxy.json, nerdctl runtime exact-pin, hub port ↔ `PORT_BASE`, field storage tier, Desktop `reconcile_compose_port` deciding mcp-os port staleness through `compose::worker_os_url_state` via the source-text guard `reconcile_compose_port_decides_staleness_through_the_compose_ssot`) — the failing test names the fix.
- CLI login-command path ↔ `consts::cli_install_path_for` — `login_command_path_matches_install_path` (auth_commands.rs) asserts the emitted command references the SSOT for macOS (`~/.local/bin/<name>`) and Windows (`<data_dir>\bin\…exe`), over default AND custom `data_dir`. Both branches are per-instance: never hand-build `~/.local/bin/speedwave`, or a dev instance emits a login command for the production binary.
- `consts::cli_install_path_for` ↔ `consts::data_dir_from_cli_exe` (producer ↔ reader of the same install path) — `data_dir_from_cli_exe_round_trips_the_install_path` and its `_windows_` twin walk the `$HOME/.speedwave*` family through both. A custom-path data dir (`/opt/sw-test`) is deliberately outside the round trip and stays `SPEEDWAVE_DATA_DIR`-only (ADR-031 §9).
- Installer dest ↔ `consts::cli_install_path_for` (**Unix only**) — `link_cli_from_copies_binary_and_sets_permissions` (setup_wizard.rs, `#[cfg(unix)]`) asserts the installed binary path equals the SSOT, and `link_cli_from_dev_instance_leaves_production_binary_alone` pins the reason it is per-instance. The Windows installer producer stays manually synced with the SSOT (the test is Unix-gated because it exercises Unix chmod); do not claim both-platform coverage.
- Desktop `build.rs` hash root ↔ `bundle::hash_inputs_resolvable` (the staged `build-context/` is hashed exactly when every input resolves there, vendored layout included, so the tag describes the tree `nerdctl build` reads) — `build_script_hashes_the_staged_context_through_the_ssot_resolver` (containers_cmd.rs); staged-side resolvability itself is pinned by `_tests/desktop/bundle-build-context.bats`.
- Bundled skills (`containers/claude-resources/skills/` — the single source; no repo-level dev copy) — `tests/bundled_skills_guards.rs`: no `model:` in any SKILL.md frontmatter (skills inherit the session model), frontmatter `name` ↔ directory name for every skill, every `/speedwave-<name>` reference ↔ an existing core skill directory, no upstream leftovers in vendored copies (`/grilling`, `/tdd`, `setup-matt-pocock-skills`, `CONTEXT*.md`, `.scratch/`, `gh`, `glab`), vendored visibility flags ↔ the ADR-087 table, `licenses-static/mattpocock-skills-LICENSE` present with pin + MIT text, `.claude/skills`/`.claude/scripts` must not exist, orchestrator `## Worker Skills` list ↔ `code-review-*` directories, shared Review Scope/Project Conventions/Output Contract blocks byte-identical across workers.
- `consts::installed_cli_filename` ↔ `windows/sweep.ps1` — `sweep_ps1_kills_all_three_target_categories` (installer_hooks.rs) asserts the script derives the filename from the data-dir basename (`Split-Path -Leaf`, the `speedwave-` strip, the `speedwave.exe` production fallback) instead of carrying a literal. The sweep must kill the exact exe `link_cli` is about to overwrite (ADR-048); a drift there means the copy fails on a locked file. Editing sweep.ps1 also means rerunning `make generate-installer-nsh`.
- CLI filename leaf ↔ bundle manifest + Tauri config — `windows_bundle_cli_asset_matches_filename_ssot` and `macos_bundle_cli_asset_matches_filename_ssot` (bundle.rs) assert `{WINDOWS,MACOS}_BUNDLED_ASSETS` and `tauri.{windows,macos}.conf.json` carry `cli/<cli_binary_filename(is_windows)>`. The bundled-asset filename SSOT is `consts::cli_binary_filename`, single-sourced from `CLI_BINARY`. The _installed_ filename is a different SSOT, `consts::installed_cli_filename`, because it carries the instance on both platforms (ADR-016) — never use the bundle name for a destination path.
- Root `[workspace.lints]` table ↔ `desktop/src-tauri/Cargo.toml` `[lints]` table ↔ `containers/proxy/Cargo.toml` `[lints]` table ↔ `crates/pii-engine/Cargo.toml` `[lints]` table (each vendored/built standalone, cannot inherit `workspace = true`) — `lint_tables_are_aligned` cross-read test.
- No raw `Command::new` outside `binary.rs` — drift detector `tests/no_raw_command_spawn.rs` (escape hatch: `// SSOT-allow: <reason>`).
- Standalone cargo workspaces (every dir with its own `Cargo.lock`: root, `desktop/src-tauri`, `containers/proxy`) ↔ the single `cargo` entry's `directories` in `.github/dependabot.yml`, every group `group-by: dependency-name` (one bump PR relocks all workspaces; a per-directory entry yields half-PRs) — `_tests/ci/dependabot-cargo-workspaces.bats`.
- Composite actions (`.github/actions/*/action.yml`) ↔ workflows (`.github/workflows/*.yml`): every external action pinned to ONE ref across both trees, and the `github-actions` entry's `directories` covers `/` plus `/.github/actions/*` with every group `group-by: dependency-name` (Dependabot's `/` scans workflows only; without the group-by a bump lands as per-directory half-PRs that the pin guard rejects) — `_tests/ci/composite-action-pins.bats`.
- Every committed `*.bats` ↔ the Makefile/workflow/composite-action/`scripts/e2e-vm.sh` line that names it — `_tests/ci/bats-suite-wiring.bats`. A suite counts as wired only when its basename is a whole token on a non-comment line, so suite basenames must be unique and every `bats` invocation must name suite files literally, never by glob or directory. The guard proves the name is present, not that the target naming it is reachable from CI. The fix for a reported orphan is adding it to a bats list in the Makefile, never deleting the guard entry.

## Manual alignments — NO automated guard; update together in the same commit

- **`scripts/build-native-macos.sh::resolve_binary_path` ↔ `_tests/desktop/native-cli-info-plist.bats::resolve_binary`** — the same three SwiftPM output candidates in the same precedence order (`.build/apple/Products/Release/`, `.build/universal-apple-macosx/release/`, `.build/release/`), then a `find` fallback. A candidate added to the script and not the test makes the test resolve a stale binary and pass; there is no cross-read test because both sides are shell.
- **`containers/entrypoint.sh` link sources ↔ `slash.rs::enrich_and_filter` lookup roots** — the popover shows an unprefixed name only when the host finds the file the entrypoint linked into the container's `~/.claude`. Search order after the project dirs: the claude-home `.claude` (`claude_home::claude_config_dir`; its links dangle on the host and fall through), the `claude-resources` of each plugin that is enabled for the project and verified (`plugin::enabled_claude_resources_dirs`, last-linked first because `ln -sfn` lets the later link win), core `claude-resources` (`linked_resource_dirs`), then integration dirs (`lookup_integration_frontmatter`). Prefixed names skip every Speedwave root. A link source added to the entrypoint but not to the lookup is hidden from the popover with no error, while the command still runs when typed.
- **`consts::installed_cli_filename` ↔ `scripts/diagnose-cli.sh`** — the script rebuilds the instance name and the CLI filename in shell so it can diagnose a named instance (`SPEEDWAVE_DATA_DIR`, else `~/.speedwave`). Same trade-off as `e2e-vm.sh`'s basename derivation (ADR-031): no Rust dependency in a troubleshooting script. Changing the suffix rule = editing both.
- **Host shell scripts and bats suites stay bash 3.2-compatible** — the macOS jobs in `test.yml` run them under `/bin/bash` 3.2 (the runner images carry no Homebrew bash, and the Makefile appends `/opt/homebrew/bin` after the caller's `PATH`); the ubuntu and Windows legs have bash 5. No test checks it: `mapfile`, `declare -A`, `${var,,}` or `[[ -v ]]` fail only when a macOS job runs that file.
- **`tzdata` in EVERY container image** (apk/apt, or a zoneinfo COPY for scratch images like the proxy) — the host TZ injected by `tz.rs::detect_host_timezone()` degrades to a numeric offset without it. `apt_noninteractive.rs` catches the debconf-prompt hang, NOT a missing tzdata. New worker image = install tzdata in the same commit.
- `windows-sys` version in `crates/speedwave-runtime/Cargo.toml` ↔ `desktop/src-tauri/Cargo.toml` (separate workspaces/lockfiles) — bump both + `cargo update -p windows-sys --precise` in both; a one-sided bump is invisible on a macOS host.
- `state_tree.rs::MessageBlock` (Rust tagged enum, `kind` snake_case) ↔ `models/state-tree.ts::MessageBlockState` ↔ renderer arm in `chat-state.service.ts::stateBlocksToMessageBlocks`. Order: Rust first, then TS mirror, then renderer arm — only the renderer's default arm (unknown kind → error block, at runtime, in front of the user) catches drift. `models/chat.ts::MessageBlock` is a separate UI view-model (discriminates on a snake_case `type` field, extra `permission_prompt` variant with no Rust counterpart), NOT the mirror. Ignore the stale in-code doc comments in `state_tree.rs` that name `models/chat.ts` as the mirror.
- `chat.rs::WireContentBlock` (user→stdin wire, **text-only**: single `Text` variant, `type` tag snake_case; image bytes go to `<project>/.speedwave/pastes/` and are referenced as `@…` text — never inline base64) ↔ TS mirror `models/chat.ts::WireContentBlock`. Rust side is snapshot-pinned; the TS half is hand-synced.
- `SLACK_OAUTH_REDIRECT_PORT` ↔ the redirect URL registered on the Slack app itself (external).
- `containers/proxy/src/forward.rs::MID_CONV_SYSTEM_REJECTION` (`capability_rejected: mid_conv_system`) ↔ the gateway contract compiled into the pinned Claude Code binary. The proxy's tests pin only the 400 envelope. Whether Claude Code still falls back on that exact `error.message` can be checked only against the binary: on every `CLAUDE_VERSION` bump, run it against a stub upstream that answers the token and confirm the resent request carries no `{role:"system"}` turn.
- `NERDCTL_FULL_SHA256_*` values ← upstream nerdctl release `SHA256SUMS` (tests pin only the 64-hex shape — a wrong-but-well-formed hash passes tests, fails at provision).
- Vulkan SDK pin: `scripts/install-vulkan-sdk.ps1` carries the SDK version plus the SHA256 of the installer, the runtime-components zip, and the extracted `x64\vulkan-1.dll` (`$RuntimeDllSha256`, ADR-085) — bump all four together; the script is the only install path (CI + setup-dev) and `stage-vulkan-runtime.sh`/`verify-bundled-assets.sh` read `$RuntimeDllSha256` from it (no second copy), but the pins themselves are manual (LunarG publishes no signature).
- Fixture-mirrored Rust↔TS tests (`derive_worker_env` ↔ hub `worker-env.test.ts`, sanitizer rule count, `ts()` timestamp shape) — each side self-tests against a hardcoded expectation; neither fails on the other's change, so update both sides' tests in the same commit. For any NEW Rust↔TS mirrored constant, add an `include_str!` cross-read test instead (grep `_matches_ts`).
- `containers/claude-resources/<type>/integrations/<dir>` name must equal a descriptor `config_key` — actually test-guarded (`integrations_directories_match_known_service_keys`), so a typo fails `make test`; listed here only as a reminder that adding the dir is the whole change.
- The plugin contract vs plugins in the `speedwave-plugins` sibling repo (cross-repo, untestable from here) — in-repo tests pin schema shape only; check the sibling repo before changing any contract surface.
- `containers/entrypoint.sh`'s `_diag_redact` secret-shape regexes ↔ `log_sanitizer.rs`'s `RULES` — a hand-ported subset (portable shell `sed -E`, one blanket `[REDACTED]` marker vs `log_sanitizer.rs`'s per-shape markers). No cross-read test exists; a new or changed `RULES` shape (e.g. a new secret-key pattern) is not automatically mirrored into `_diag_redact` and must be ported by hand in the same commit.
- `mcp-servers/os/src/tools/validation.ts::ISO8601_RE` ↔ SharedCLI `DateUtils.swift::parseISODateInput` patterns — the TS gate and both EventKit CLIs accept the same three shapes: zero-padded `YYYY-MM-DD`, `YYYY-MM-DDTHH:MM:SS[.fff]`, and the same with `Z`/`±HH:MM`. A shape TS accepts but Swift rejects fails late as `CLIError.invalidDate` from the host CLI (how offset-less event times used to fail); a shape Swift accepts but TS rejects is unreachable. No cross-read test (TS vs Swift).
